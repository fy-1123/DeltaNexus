# 网格（格子背包）底层架构转型方案

> 目标：**彻底抛弃占位物设计**，把「网格」从“平铺槽位 + 派生标记”转为“一等公民容器 + 派生占用”，
> 让复制、重叠、识别失效这一类问题在结构上不可能发生。
> 版本策略：0.3.0Beta 未公开发布，协议代号保持 `dn3`（对外不新增版本号），内部握手加包表指纹防混装。

---

## 一、病因诊断（当前为什么“bug 一堆”）

| # | 根因 | 表现 | 现状 |
| :--- | :--- | :--- | :--- |
| 1 | **同一协议代号下存在多个不同包表的构建** | 混合安装时 Forge 放行连接、包 ID 平移 → 「A 包当 B 包解」：出售识别不出、目录/界面莫名为空 | 已修：握手串改为 `dn3+<包表指纹>`，混装直接拒绝（本条是「连出售都识别不来」最可能的直接原因） |
| 2 | **派生态（占位物）写进物品层** | 任何绕过模组的写入（漏斗/管道/其他模组/指令/死亡/维度）都能打破不变式；旧实现的“修复”会重写格子 = 造物品 | 已止血：不再写占位物（只清残留）；但“平铺槽位仍被当作网格”这一层没变 |
| 3 | **两个写者并行**（点击时收敛 + 每 tick 重排） | 过期计划把已移走的物品写回 → 复制；owner 表过期 → 重叠 | 已止血：每 tick 只做只读校验、事务改为影子数组 + CAS + 守恒 |
| 4 | **几何口径多处实现** | 「服务端认为合法、客户端认为非法」→ 假性复制/重叠；2x2 落进 1x1 | 已收敛：`GridLayout.derive` 成为唯一推导（求解/写入/校验/下发共用） |
| 5 | **外部写入无强约束** | 管道/漏斗/其他模组往足迹格塞东西 → 抖动（现在会被合法位移，不再损坏） | 半修：只有自家容器有软闸（`GridFootprints`） |
| 6 | **手动放置与自动求解边界不清** | “点 A 却跳到 B”“放进去又弹出来” | 未修：需要“放置即校验、失败即拒绝”的单一入口 |

**结论**：1–4 已止血，但 2/5/6 的**根**在于“平铺槽位被当成网格用”。
只要还允许「随便往某个格子里 set 一个 ItemStack」，网格正确性就永远靠事后协调。
→ 转型目标：**让“往格子里写”这件事本身变成非法的**，只允许“通过网格 API 落位”。

---

## 二、目标架构

### 1. 数据模型（唯一真相）

```text
GridInventory(容器)                       # 一等公民：仓库 / 安全箱 / 玩家背包（见 §4 的取舍）
├── width, rows, size
├── usableMask                            # 未解锁格
├── entries: Map<Cell, Entry>             # 只存“锚点”，key = 容器内索引
│     Entry{ ItemStack stack, GridDim dim, boolean rotated }
└── revision: long                        # 任何变更 +1，缓存与同步都以它为准
```

- **占位物不存在**：足迹内的非锚点格就是空。
- **占用是算出来的**：`derive()` 由 `entries` 直接给出每格归属；不落盘、不同步、不可能失配。
- **旋转存在 Entry 上**（不是物品 NBT）：锚点是物品的“家”，随锚点走；只有玩家背包需要妥协（见 §4）。
- **序列化**：只写 `entries`（锚点 + 物品 + 尺寸 + 旋转），**绝不写派生态**。

### 2. 唯一不变式（可判定）

1. 每个非空格都属于某个 Entry 的锚点；
2. 覆盖格必为空；
3. 任意两 Entry 的足迹不相交，且足迹整体在界内、在可用格上、不跨行分区；
4. 物品总量守恒（事务级断言）。

### 3. 唯一入口（这是转型的关键）

```java
// 所有写入都必须经过它；没有第二个写格子的地方
sealed interface GridOp permits Place, Move, Take, Merge, Split, Rotate, Sort
GridInventory.apply(GridOp op, Actor actor) -> Result
```

- **事务**：先在影子数组上算完 → 校验不变式 + 守恒 → 一次性写回 → `revision++`。
- **CAS**：op 携带前置条件（源格内容、目标足迹状态），不满足即整条 op 失败（不是部分成功）。
- **失败即拒绝**：手动放置走 `canPlace(cell, stack, dim)` 先问后放；不允许“先放进去再被挪走”。
- **锁**：按玩家/容器 canonical 顺序加锁；禁止裸 `synchronized`。

### 4. 槽位与容器怎么接

| 容器 | 存储 | 说明 |
| :--- | :--- | :--- |
| 仓库 / 安全箱 | **`GridInventory` 原生持有** | 存档直接存 entries；`ItemStackHandler` 只作为**只读视图**暴露给旧代码（只读视图不允许 set） |
| 玩家背包 | 基于原版 `Inventory` 的**派生视图** | 不接管存储（死亡掉落/其他模组/盔甲副手都要原版语义）；占用派生、写入经 `GridOp`；**旋转只能进物品 NBT**（唯一妥协点，见下） |
| 其他模组容器 | 只读装饰或要求适配器 | 不接管，避免“接管但不完全控制” |

**旋转的取舍**：仓库/安全箱放 Entry（干净）；玩家背包只能放物品 NBT（否则槽位一移动就失配）。
若你要求“全链路去 NBT 化”，唯一可行方案是连玩家背包也换成自有容器——那会破坏与原版/其他模组的兼容，
**不建议**（稳定性优先）。

### 5. 客户端与服务端

- 槽位同步回归原版语义：**只有锚点格有物品，覆盖格为空**。
- 布局同步：`SyncGridLayoutPacket`（已实现）就是权威几何：`{containerId, revision, entries[锚点,宽高,旋转,行宽]}`。
- 客户端只做两件事：按布局画大图标；把点击指向锚点。**客户端永不自行推导几何、永不发明物品**。
- 混装防护：握手版本 = `dn3+<包表指纹>`（已实现）。

---

## 三、安全网（不依赖“写对”，而是“写错就报”）

1. **守恒断言**：每次事务前后总量差 = 明确离开容器的量，否则整条事务回滚 + ERROR 日志（已实现）。
2. **引用共享断言**：同一 `ItemStack` 实例不得出现在两个格（已实现）。
3. **布局校验**：任何时刻可对容器跑一次 `derive()` 并比对不变式，不一致即收敛（已实现）。
4. **只读视图**：旧代码拿到的 handler 只读（转型后），让“顺手 set 一下”在编译/运行期就失败。
5. **回归清单**（每次改动必跑）：见 §6。

---

## 四、迁移

1. 新版读旧数据：旧平铺仓库 → 逐格扫描，非空且非占位物者作为候选 Entry（尺寸取配置、旋转取 NBT）。
2. **冲突处理**（顺序固定，结果确定）：按面积降序尝试原位落位 → 失败则找首个空位 → 再失败则**保留原位但退化为 1x1**（不移动玩家物品，避免“上线就少东西”）。
3. 旧 `blocked_slot` 一律丢弃（不迁移）。
4. 迁移是一次性的：写回新格式 + `format_version` 标记；旧格式文件保留一份 `.bak`。
5. 玩家背包不迁移（本来就是原版存储）。

---

## 五、落地阶段（每阶段都可独立验收、可回滚）

| 阶段 | 内容 | 验收 |
| :--- | :--- | :--- |
| P0（已完成） | 止血：每 tick 只校验、事务影子+CAS+守恒、`derive` 唯一几何、去占位物写入、握手指纹、布局下发 | `build` 通过；21 个内核 GameTest 通过；**左右两侧 jar 同构建**时不再出现静默错包 |
| P1 | `GridInventory` 原生接管**仓库 + 安全箱**（entries 存储 + 只读 handler 视图 + GridOp 入口），旧平铺格式自动迁移 | 仓库/安全箱：放入、取出、Shift、数字键、Q、滚动、交易交付、**卖出识别与结算**全部正常；存档里再无 `blocked_slot`；重启后布局不变 |
| P2 | 玩家背包改为派生视图 + `GridOp` 写入（旋转继续走 NBT） | 背包/口袋/快捷栏规则、跨格物品拿起放下、死亡掉落、维度切换、其他模组交互不缺件不复制 |
| P3 | 清理历史包袱：删除 `isSlave/master_slot` 相关代码路径与占位物物品（保留注册以兼容旧存档读取） | 全仓库无 `is_slave` 写入路径；旧存档加载仍正确 |

---

## 六、回归清单（每次改动必跑，含“出售识别”）

1. 交易行：**上架 → 识别可回收（绿框 + tooltip 单价）→ 选中 → 出售 → 结算金额/库存正确**。
2. 跨格物品：拿起（点任意格）、放下、Shift 快捷移动、数字键换位、Q 丢弃、R 旋转。
3. 仓库：滚动、锁定格、升级扩容后布局正确。
4. 安全箱：点击、Shift、1x1 限制、NBT 限制。
5. 数据：自动保存 / 手动重启 / 死亡重生 / 维度切换 / 断线重连 —— 物品数量逐件核对（守恒）。
6. 混装：旧客户端连新服务端（应当被**明确拒绝**，而不是进服后错乱）。

---

## 八、进度（滚动更新）

### 已完成

- **P0 止血**（已合入，`build` 通过）：每 tick 只做只读一致性校验（不一致才收敛）、事务改为「影子数组 + CAS + 守恒 + 引用共享校验 + 原子写回且失败即整体放弃」、
  `GridLayout.derive` 成为唯一几何推导、停止写入占位物（只清历史残留）、布局下发到客户端、握手串加包表指纹（`dn3+<指纹>`，混装明确拒绝）、
  网格异常全链路隔离（不再带崩同一 tick 的其他系统，如交易目录与出售结算）。
- **P1 内核**（已合入，`build` 通过，**尚未接线**，见下）：新包 `com.deltanexus.system.grid.v2`
  - `GridEntry` —— 条目（物品 + 实际尺寸 + 姿态），**尺寸与旋转属于条目，不属于物品 NBT**；
  - `GridInventory` —— 一等公民容器：只存锚点、占用现算、`apply(GridOp)` 唯一入口（影子推演 → 不变式校验 → 守恒校验 → 提交或整体拒绝）、
    `validate()` 随时可判定、`describe()` 可打印现场；
  - `GridOp` —— Place / Take / Move / Rotate / Compact（**全容器唯一允许的“全局移动”入口，且必须显式触发**）；
  - `GridHandlerBridge` —— 把 v2 网格暴露成旧 `ItemStackHandler` 形态（调用点零改动），序列化走新格式、读到旧格式自动迁移；`PlayerDataImpl` 的**仓库存储已切换为 v2**（占位物/派生标记彻底消失）`r`n  - `GridStorage` —— 新格式（只写锚点：`cell/item/w/h/rotated` + `format_version`）与**旧平铺格式迁移器**（`migrateFrom`：按面积降序，原位 → 首个空位 → 退化 1x1 保留原位，占位物直接丢弃，剥离物品上的旧网格 NBT）。

> ⚠️ **本轮不影响游戏内行为**：v2 内核目前没有任何调用点（死于未接线），仓库/安全箱/背包仍走旧路径。
> 这是刻意的——先在旁边把新引擎建好并保证编译通过，再一个容器一个容器切换，任何一步都能回滚。

### 本轮事故与纪律（务必遵守）

- **事故**：为「握手指纹」把通道创建挪到包表注册之后，同时 `register()` 内部改走 `channel()` 守卫 →
  模组在**构造期直接抛 `IllegalStateException`，整个模组加载不起来**。
  表现是“所有功能一起坏”（含“出售识别不了”），而 `gradlew build` 完全看不出问题——编译通过 ≠ 能加载。
- **纪律**：**每次改动必须跑 `gradlew runGameTestServer`**（它会真的启动一个服务端、加载模组、跑 21 个测试）。
  只跑 `build/compileJava` 不足以作为交付证据。
- **修复**：通道恢复为静态创建，版本串改为**惰性求值**（`PacketHandler::protocolWithFingerprint`），
  握手时才计算指纹；`register()` 内直接使用 `CHANNEL`。**不要**回退成“注册后再建通道”的顺序。

### 下一步（P1 接线，建议顺序）

1. **仓库切换到 v2**（`PlayerDataImpl`）：
   - 新增字段 `GridInventory warehouseGrid`，`getWarehouseHandler()` 返回**只读视图**（`insertItem`/`extractItem`/`setStackInSlot` 一律拒绝或走 `GridOp`），
     让“顺手 set 一下”这类写入在运行期直接失败——这正是旧设计一切麻烦的源头；
   - 存档：`serializeNBT` 写 `GridStorage.write(grid)`；`deserializeNBT` 先试 `GridStorage.read(...)`，失败则 `GridStorage.migrateFrom(旧数据)` 并立刻回写新格式（保留 `.bak`）；
   - 上下文：解锁位图 → `usableMask`；`setUnlockedSlots/unlockUpTo/resizeWarehouse` 变更新格式（缩格时若被占用则拒绝并提示，不再“把物品挤掉”）。
2. **菜单/服务端接上**：`WarehouseMenu` 的仓库视口槽位改为「锚点显示物品、覆盖格为空」的只读槽；
   `clicked` 直接翻译成 `GridOp`（点击覆盖格 → 锚点；Shift/数字键/Q 同理），服务端在 `GridInventory` 上执行；
   `GridService` 的收敛逻辑改为“校验 `validate()` + 必要时 `compact()`（只由显式入口触发）”。
3. **安全箱同法切换**（宽度可变 → `GridInventory(width, rows)`）。
4. **玩家背包**：保持原版 `Inventory` 存储，但写入走 `GridOp` 语义（占用派生），旋转仍走物品 NBT（唯一妥协点，见 §四）。
5. **删除旧机制**：`GridTags.is_slave/master_slot` 写入路径、`GridMutation` 占位物分支、`GridLayout` 中占位物校验、`C2SPickupGridStackPacket`（或保留为兼容空实现）。
6. **每一步的验收**：`gradlew build` 通过 + §六 回归清单逐条过（**第一条就是出售识别与结算**）。

### 回滚点

- v2 内核与旧引擎并存：接线以「单容器 + 单次提交」为单位，任何一步异常都能靠 `GridStorage.read` 回退旧格式（`.bak` 保留原文件）。


- 不接管任意模组容器（除非对方提供适配器）。
- 不把旋转写进玩家背包以外的派生态与物品层混合（统一“Entry 或 NBT”二选一）。
- 不做每 tick 全局重排（既是性能问题，也是“物品自己跳走”的体验问题）。
- 不为“同一个协议代号下的不同包表”做兼容——一律用握手指纹拒绝。

---

## 七、明确不做

- 不接管任意模组容器（除非对方提供适配器）。
- 不把旋转写进玩家背包以外的派生态与物品层混合（统一“Entry 或 NBT”二选一）。
- 不做每 tick 全局重排（既是性能问题，也是“物品自己跳走”的体验问题）。
- 不为“同一个协议代号下的不同包表”做兼容——一律用握手指纹拒绝。
- 不在未定位现有 bug 的前提下改存档格式：先让当前基线在单人里跑通（出售识别 / 结算 / 跨格操作），再动 P1 接线。