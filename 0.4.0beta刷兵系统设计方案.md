# DeltaNexus 刷兵系统

## 一、设计原则

1. **零主动** — 不监听事件、不自动刷兵、不定时、不循环、不判断时机。
2. **单次执行** — 每次调用只刷一次，执行完即结束。
3. **管理员配置一切** — 实体、点位、数量、条件、NBT、行为全部通过指令配置。
4. **无波次、无冷却** — 节奏完全由外部控制。
5. **配置跟着世界文件夹走** — 克隆世界自动携带，世界名后缀无关。
6. **不持有世界引用** — 所有配置以「世界文件夹路径」为键。

---

## 二、配置存储与组别划分

### 2.1 配置分布

```
config/deltanexus/                    ← 全局层
├── global.json                       ← 全局保护（默认关闭）
└── logging.json                      ← 日志

saves/<世界名>/deltanexus/             ← 世界层
├── spawners.json                     ← 刷兵器（含点位、实体、规则）
└── pointgroups.json                  ← 点位组
```

### 2.2 组别划分 — 命名前缀

系统不做区域管理。地图内不同组别用命名前缀区分：

```
pvp_gate_01
pvp_gate_02
boss_pad_01
spawn_center_01
```

系统不关心 `pvp_` 是什么“区域”，它只知道这是一个名字。批量操作靠通配符，组别隔离靠前缀。

### 2.3 克隆世界加载逻辑

```
世界加载
  → 检查 saves/<世界名>/deltanexus/ 是否存在
     → 存在：加载世界内配置
     → 不存在：使用全局默认
  → 世界名是什么？完全不关心
```

| 时机 | 行为 |
|------|------|
| 世界加载 | 读取 `saves/<世界>/deltanexus/` |
| 世界运行中 | 内存缓存，指令修改同步写文件 |
| 世界卸载 | 清空缓存，文件保留 |
| 克隆世界被重置 | 不持有世界引用，无需清理 |

---

## 三、指令

### 3.1 指令树

```
/dn
└── spawner
    ├── new <名字>                              # 创建刷兵器
    ├── del <名字>                              # 删除刷兵器
    ├── list [pattern]                          # 列出刷兵器
    ├── info <名字>                             # 查看刷兵器详情
    ├── set <名字> <key> <value>                # 设置刷兵器字段
    ├── nbt <名字> <key> <value>                # 设置生物 NBT
    ├── point
    │   ├── add <名字> <点位名> [x y z]         # 添加点位（不填坐标默认执行者位置）
    │   ├── del <名字> <点位名|pattern>         # 删除点位
    │   ├── list <名字> [pattern]               # 列出点位
    │   ├── move <名字> <pattern> x,y,z         # 批量位移
    │   ├── radius <名字> <pattern> <n>         # 批量设置散布半径
    │   ├── enable <名字> <pattern>             # 批量启用
    │   └── disable <名字> <pattern>            # 批量禁用
    ├── group
    │   ├── add <组名> <pattern>                # 把匹配的点位归入组
    │   ├── del <组名>                          # 删除组
    │   └── list                                # 列出所有组
    ├── run <名字> [世界]                       # 执行刷兵（不填世界默认当前世界）
    ├── preview <名字>                          # 预览会刷什么、刷多少、在哪刷
    ├── dry-run <名字>                          # 走完整流程但不生成实体
    └── log <level>                             # 设置日志级别
```

一级命令只有 **1 个**：`spawner`。

### 3.2 权限

| 权限节点 | 等级 |
|---------|------|
| `deltanexus.spawner` | 2（OP） |
| `deltanexus.run` | 2（OP） |

所有 `/dn spawner` 指令需要权限等级 **2**。

### 3.3 刷兵器 = 实体 + 点位 + 规则

每个「刷兵器」是一个完整的刷兵单位。

```
/dn spawner new pvp_zombie
/dn spawner set pvp_zombie entity minecraft:zombie
/dn spawner set pvp_zombie count 3-5
/dn spawner nbt pvp_zombie name "亡灵战士"
/dn spawner point add pvp_zombie pvp_gate_01 10 64 0
/dn spawner point add pvp_zombie pvp_gate_02
/dn spawner point radius pvp_zombie pvp_gate_* 2
```

### 3.4 点位组 — 复用一批点位

```
/dn spawner group add pvp_all_gates pvp_gate_*
/dn spawner set boss_summon point group:pvp_all_gates
```

### 3.5 执行

```
/dn spawner run pvp_zombie
/dn spawner run pvp_zombie arena_x7f
```

不填世界默认当前执行指令的世界。执行一次，返回结果，结束。

### 3.6 预览与试运行

```
/dn spawner preview pvp_zombie     # 显示会刷什么、刷多少、在哪刷
/dn spawner dry-run pvp_zombie     # 走完整流程但不生成实体
```

### 3.7 日志级别

```
/dn spawner log <level>
```

设置日志级别（`off` / `error` / `warn` / `info` / `debug`），写入 `config/deltanexus/logging.json`。

---

## 四、配置内容

### 4.1 全局 `config/deltanexus/global.json`

```json
{
  "enabled": false,
  "blacklisted_world_patterns": ["creative_*", "test_*"],
  "global_limits": {
    "max_total_entities_per_world": 200,
    "max_entities_per_chunk": 20,
    "min_tps_to_allow_spawn": 15.0,
    "min_free_memory_percent": 20
  }
}
```

| 字段 | 作用 |
|------|------|
| `enabled` | 全局保护开关，**默认 false（关闭）** |
| `blacklisted_world_patterns` | 禁止刷兵的世界（通配符） |
| `max_total_entities_per_world` | 单世界实体总数上限 |
| `max_entities_per_chunk` | 单区块实体数上限 |
| `min_tps_to_allow_spawn` | TPS 低于此值拒绝刷兵 |
| `min_free_memory_percent` | 内存低于此值拒绝刷兵 |

`enabled = false` 时，以下全部不生效，刷兵不受任何全局限制。
`enabled = true` 时，以下全部生效。

### 4.2 日志 `config/deltanexus/logging.json`

```json
{
  "log_level": "info",
  "log_successful_spawns": true,
  "log_failed_spawns": true,
  "log_condition_rejections": true,
  "log_to_console": true,
  "log_to_file": true,
  "log_file_path": "logs/deltanexus_spawn.log"
}
```

### 4.3 刷兵器 `saves/<世界名>/deltanexus/spawners.json`

```json
{
  "pvp_zombie": {
    "entity": "minecraft:zombie",
    "entity_pool": null,
    "nbt": {
      "CustomName": "{\"text\":\"亡灵战士\"}",
      "CustomNameVisible": true,
      "Attributes": [{ "Name": "generic.max_health", "Base": 30 }],
      "HandItems": [
        { "id": "minecraft:iron_sword", "Count": 1 }, {}
      ]
    },
    "count": "3-5",
    "point": "group:pvp_all_gates",
    "points": {
      "pvp_gate_01": { "x": 10, "y": 64, "z": 0, "radius": 2.0, "enabled": true },
      "pvp_gate_02": { "x": -10, "y": 64, "z": 0, "radius": 2.0, "enabled": true }
    },
    "condition": {
      "difficulty": ["normal", "hard"],
      "player_online_min": 1
    },
    "safety": {
      "require_solid_ground": true,
      "avoid_lava": true,
      "avoid_water": true,
      "max_attempts_per_entity": 5
    },
    "behavior": {
      "spawn_effect": {
        "particle": "minecraft:smoke",
        "sound": "minecraft:entity_zombie_ambient"
      },
      "on_spawn_commands": ["effect give @s minecraft:speed 60 1"],
      "glowing": false,
      "persistent": true,
      "silent": false,
      "no_ai": false
    }
  }
}
```

### 4.4 点位组 `saves/<世界名>/deltanexus/pointgroups.json`

```json
{
  "pvp_all_gates": ["pvp_gate_01", "pvp_gate_02", "pvp_gate_03"],
  "boss_pads": ["boss_pad_01"]
}
```

### 4.5 字段总览

| 字段 | 说明 | 可选值 |
|------|------|--------|
| `entity` | 刷什么生物（单一） | 原版/模组实体 ID |
| `entity_pool` | 刷什么生物（随机池） | 带权重的实体列表 |
| `nbt` | 生物属性 | 完整 NBT |
| `count` | 数量 | 固定值 / 范围 / `per-point` |
| `point` | 引用点位组 | `group:组名` |
| `points` | 自有点位 | 点位名 → 坐标 |
| `condition` | 条件 | 时间、天气、难度、人数 |
| `safety` | 安全检测 | 地面、岩浆、水、尝试次数 |
| `behavior` | 生成后行为 | 特效、命令、永久、发光 |

### 4.6 随机实体池写法

```json
"entity_pool": [
  { "id": "minecraft:zombie", "weight": 5 },
  { "id": "minecraft:skeleton", "weight": 3 },
  { "id": "minecraft:spider", "weight": 2 }
]
```

按权重随机分配。

---

## 五、核心结论

1. **根指令 `/dn`，一级只有 1 个：`spawner`。**
2. **所有指令需要权限等级 2（OP）。**
3. **刷兵器 = 实体 + 点位 + 规则**，一个名字搞定一切。
4. **地图内不同组别用命名前缀** — `pvp_`、`boss_`、`spawn_`。
5. **`/dn spawner run` 只刷一次** — 节奏由外部控制。
6. **无冷却、无波次、无定时** — 完全由外部控制。
7. **全局保护默认关闭** — 管理员按需开启。
8. **配置跟着世界文件夹走** — 克隆世界自动携带，世界名后缀无关。
9. **模组零主动** — 不监听、不自动、不持有世界引用。